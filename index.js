// exercise 1
// complete the function so taht the result of the function replaces all occurences
// of the value received as toBeReplaced by the value of the parameter newPart
// example: replaceApparences('this+is+line+of+text', '+', ' ') -> 'this is a line of text'
const replaceOccurences = (str, toBeReplaced, newPart) => {
  // add your code here
}

// exercise 2
// complete the function so that the result of the function is
// the number of vowels in "str"
// example: countVowels('tapas are nice') -> 6
const countVowels = (str) => {
  //add your code here
}

// exercise 3
// complete the function so that the result of the function
// is the first "n" characters of "str" plus the last
// "n" characters of "str"
// example: getFirstAndLast('hey this is a test', 4) -> 'hey test'
const getFirstAndLast = (str, n) => {
  //add your code here
}

module.exports = {
  replaceOccurences,
  countVowels,
  getFirstAndLast,
}